// const AST_TESTS = [
//   ['function f(x) {return x}',
//   '(FunDec f (Params x) (Block [(Return (VarExp x))]))']
// ]
//
// AST_TESTS.forEach((a) => {
//  assert.equal(parse(a[0]).toString(), a[1])
// });
// const ast = parse('function f(x) {return x}');
// const expected = "(FunDec f (Params x) (Block [(Return (VarExp x))]))"
//
// assert(ast, expected);
