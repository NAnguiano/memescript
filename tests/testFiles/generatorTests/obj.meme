wow testClass {
  such (p1, p2, p3) {
  	dicks.out(p1);
    dicks.out(p2);
    dicks.out(p3);
  }

  so shine () {
  	dicks.out("Double rainbow");
  }

  so woah () {
    ermahgerd x = 10;
  }
}

ermahgerd a = very testClass(10, 11, 12);
