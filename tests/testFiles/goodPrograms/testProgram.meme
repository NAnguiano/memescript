overlyattachedgirlfriend.jpg ARGH;
argh = true;
ermahgerd brbcrying;
brbcrying = true;

wow doge {
  such (paw, color, tail) {
    dicks.out(paw);
    dicks.out(color);
    dicks.out(tail);
  }
  so grace (me) {
    dicks.out(me);
  }
}
